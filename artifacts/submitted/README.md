# Submitted reference artifacts

`supporting_data/` is extracted byte-for-byte from the CMT supporting-data ZIP. Do not reformat, normalize line endings, or overwrite these files. They are reference evidence and are not used as prediction inputs.
