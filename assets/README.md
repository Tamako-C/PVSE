# Model and dataset assets

The trained ResNet-12 backbone is distributed through the GitHub Release. FEAT and all datasets are connected through documented local-path interfaces.

Canonical model identities are recorded in `checkpoints.json`. Every paper runner validates the expected checkpoint digest before execution.
